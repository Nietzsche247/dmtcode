const cors = { 'Access-Control-Allow-Origin': '*', 'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type' };
const json = (b: unknown, s = 200) => new Response(JSON.stringify(b, null, 2), { status: s, headers: { ...cors, 'Content-Type': 'application/json' } });
const safe = (t: string) => { try { return JSON.parse(t); } catch { return t; } };
function b64url(i: string | Uint8Array): string { const b = typeof i === 'string' ? new TextEncoder().encode(i) : i; let s = ''; for (const x of b) s += String.fromCharCode(x); return btoa(s).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, ''); }
function pem(p: string): ArrayBuffer { const b = p.replace(/-----BEGIN PRIVATE KEY-----/, '').replace(/-----END PRIVATE KEY-----/, '').replace(/\\n/g, '').replace(/\s+/g, ''); const r = atob(b); const u = new Uint8Array(r.length); for (let i = 0; i < r.length; i++) u[i] = r.charCodeAt(i); return u.buffer; }
async function tok(sa: { client_email: string; private_key: string }): Promise<string> { const n = Math.floor(Date.now() / 1000); const h = b64url(JSON.stringify({ alg: 'RS256', typ: 'JWT' })); const c = b64url(JSON.stringify({ iss: sa.client_email, scope: 'https://www.googleapis.com/auth/analytics.edit', aud: 'https://oauth2.googleapis.com/token', exp: n + 3600, iat: n })); const si = `${h}.${c}`; const k = await crypto.subtle.importKey('pkcs8', pem(sa.private_key), { name: 'RSASSA-PKCS1-v1_5', hash: 'SHA-256' }, false, ['sign']); const g = new Uint8Array(await crypto.subtle.sign('RSASSA-PKCS1-v1_5', k, new TextEncoder().encode(si))); const a = `${si}.${b64url(g)}`; const r = await fetch('https://oauth2.googleapis.com/token', { method: 'POST', headers: { 'Content-Type': 'application/x-www-form-urlencoded' }, body: new URLSearchParams({ grant_type: 'urn:ietf:params:oauth:grant-type:jwt-bearer', assertion: a }) }); const t = await r.text(); if (!r.ok) throw new Error(`token ${r.status}: ${t}`); return JSON.parse(t).access_token; }
Deno.serve(async (req) => { if (req.method === 'OPTIONS') return new Response('ok', { headers: cors }); try { const pid = Deno.env.get('GA4_PROPERTY_ID'); const sa = JSON.parse(Deno.env.get('GA4_SERVICE_ACCOUNT_JSON') || '{}'); const t = await tok(sa); const base = `https://analyticsadmin.googleapis.com/v1beta/properties/${pid}/keyEvents`; const events = ['notify_me_signup']; const created: unknown[] = []; for (const eventName of events) { const r = await fetch(base, { method: 'POST', headers: { Authorization: `Bearer ${t}`, 'Content-Type': 'application/json' }, body: JSON.stringify({ eventName }) }); created.push({ eventName, status: r.status, body: safe(await r.text()) }); } const l = await fetch(base, { headers: { Authorization: `Bearer ${t}` } }); return json({ pid, serviceAccount: sa.client_email, created, keyEventsList: safe(await l.text()) }); } catch (e) { return json({ error: String(e) }, 500); } });
