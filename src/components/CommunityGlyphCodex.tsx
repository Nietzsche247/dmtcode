// Retired component. The legacy `glyphs` table has been superseded by
// `symbol_submissions` as the single source of real convergence data.
// This stub is preserved so historical imports do not break the build.
export const CommunityGlyphCodex = () => null;
