# Roadmap

- [x] Migration: laser_protocol columns + CHECKs on retreats
- [ ] Insert nine retreat centers
- [ ] /retreats/laser-protocol page + route ordering (before /retreats/:id)
- [ ] Prerender handler (before UUID branch), spa-guard, sitemap
- [ ] Verify counts, CHECK rejection, UUID detail pages still resolve
